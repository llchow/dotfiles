- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.tmux: tmux
    ~/.vimrc: vimrc
    ~/.vimrc_f: vimrc_f
    ~/.vim: vim
    ~/.note: note/
    ~/bin: bin/
    ~/.Xdefaults: Xdefaults

- shell:
  - [git submodule update --init --recursive, Updating submodules]
  - command: vim +PluginInstall +qall
    stdin: true
    stdout: true
    stderr: true
